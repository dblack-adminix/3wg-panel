#!/usr/bin/env python3
# Disabled: this patch was only cosmetic and could break deploy because it injects CSS/HTML into app.py.
# The real logo placement is handled by scripts/apply_logo_patch.py.
print('Visible classic UI patch skipped')
