# 3WG Panel

3WG Panel — node panel for managing WireGuard and AmneziaWG peers with QR/config generation, HTTPS via Caddy, and installer bundles.

Development server:

cz-prg-01.nodax.eu

Deploy:

bash /srv/3wg-panel/scripts/deploy.sh
